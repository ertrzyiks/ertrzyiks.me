# Implementation Plan

Prioritized list of items yet to be implemented or fixed. Check specs/* for behavioral contracts.

## Completed

- [x] **Narrative events engine + Dialog component + Stage 1 script (specs 07, 09)** — `core/narrative/index.ts`: pure, testable engine. Trigger union `{turn | tileReached | lastUnitDefeated | unitDefeated(predicate) | proximity}`; `triggerMatches(trigger, state, action)` gates each kind to the action type that can cause it (turn→StartTurn, tileReached/proximity→Move, *Defeated→TakeDamage) — this gating is what stops a "unit defeated" beat firing on the Spawn events before a unit exists. `NarrativeEngine.evaluate(state, action)` returns newly-fired events in **script definition order** (= queue order), marks non-repeatable ones fired (fire-once), `hasFired`/`reset` (reset for stage reload). `proximity` range defaults to the watcher unit's `sightRange` (drives the Wanderer beat). **Presentation:** `shared/dialog_box.ts` `DialogBox` — screen-space modal (speaker bold + word-wrapped body), full-screen interactive backdrop that swallows board taps, one tap advances one line, `onComplete` on last-line dismissal. **Wiring:** `main/narrative/stage1.ts` `createStage1Narrative(humanId, wandererId)` = the four spec-09 beats verbatim. `MainWorld` subscribes a **peer world-observable subscriber** (`onNarrativeUpdate`) registered *before* `scenario.start()` so it sees the first StartTurn (Turn-1 beat). **The pause mechanism is the serial `Observable`:** the narrative subscriber does NOT call `done()` until every queued beat is dismissed, which holds the whole pipeline — so a win/lose `GameEnd` that the Game queued *behind* a winning `Move` waits until the village/defeat dialog is read (spec 07 "game ends after the dialog is dismissed"). Board input gated on `!dialogActive` (plus the backdrop). No double-dispatch of GameEnd — win/lose stays owned by `core/conditions`; `endsStage` on a beat is documentation only. Tests: `core/narrative/index.test.ts` (18: all five trigger kinds incl. action-gating, fire-once, definition-order queue, repeatable, reset). **184 tests, tsc clean, astro check 0 errors, build clean.**
- [x] **Fix `World.tileBySection(name)`** — now finds tile by sectionName. Tests passing.
- [x] **Fix `Movable.step(cost)`** — now subtracts cost from movementPoints. Tests passing.
- [x] **Fix `Explorer.takeActions()`** — now checks `canMove()` before dispatching Move. Tests passing.
- [x] **Fix `World.unitsOf(player)`** — now filters by owner id. Tests passing.
- [x] **Unit ownership** — `UnitPosition` carries `owner: Player`; reducer stores it on Spawn.
- [x] **Replenishment on turn start** — reducer calls `replenish()` on all units belonging to the current player at StartTurn.
- [x] **Fog of war** — `Sightful` mixin, `revealedTiles` per player in State, hex fog Graphics layer in renderer. Tests passing.
- [x] **Wolf unit** — `Wolf = Sightful(Movable(Damageable(Unit, 15), 2), 1)`. Tests passing.
- [x] **Stage 1 scenario (partial)** — Scenario spawns Whirley + a wolf pack (1 PackLeader at `wolf_1`, 2 PackFollowers at `wolf_2/3`). Human turn auto-passes (no input yet).
- [x] **Wolf Pack Leader / Follower units** — `PackLeader = Leader(Sightful(Movable(Damageable(Unit,20),2),2))`, `PackFollower = Follower(Sightful(Movable(Damageable(Unit,15),2),1))` in `main/units/wolf.ts`. Core role mixins `Leader`/`Follower` + `isWolf`/`isPackLeader`/`isPackFollower` guards in `core/units/pack.ts`. (Old generic `Wolf` removed — single source of truth.)
- [x] **Movement AI helpers** — `core/player/movement.ts`: `createMoveContext` (bounds from actual tiles, occupancy from units), `validDirections`, `directionToward`, `directionAway`, `randomValidDirection` (injectable rng, no-backtrack). Reusable by all enemy behaviors.
- [x] **Pack movement AI + dissolution** — `core/player/pack_behavior.ts` `PackBehavior`: leader wanders (random valid dir, avoids backtracking across turns via `PackMemory`), followers step toward the living leader (stay if adjacent), and when no living leader is present the pack dissolves to wandering. Leader-first action order. Wired into `main/scenario.ts`. **Attack-on-adjacency is intentionally NOT emitted** — it depends on the combat system (TakeDamage), a later increment; see Stage 2 list.
- [x] **Fix `State.players` type** — was `Array<{id;name}>`, causing pre-existing `tsc`/`astro check` type errors in `player_store.test.ts` and `reducers/index.test.ts` (currentPlayer is `Player`, needs `color`). Now `Array<Player>`. Build + typecheck clean.
- [x] **Combat system core (spec 04)** — `Damaging(Base, damage, attacksPerTurn=1)` mixin in `core/units/damaging.ts` (replaces the old unused `IDamaging` interface) gives a fixed `damage`, attack-charge budget (`canAttack`/`useAttack`), and `replenish()` chaining. `isDamaging`/`isDamageable` guards. Reducer handles `GameEventType.TakeDamage`: spends the attacker's charge, applies `takeDamage`, and removes the resolved target if `!isAlive()` (only the target — bystanders reading 0 HP pre-replenish are left alone). `player_store` translates `PlayerAction.Attack` → `TakeDamage` (resolves target by hex from the full roster, rejects friendly-fire / non-adjacent / no-charge). Tests passing.
- [x] **Proxy exposes `allUnits`** — `createPlayerStore.proxyState` now adds `allUnits` (full roster) while keeping `units` filtered to the owner. `State.allUnits?` is optional (base store leaves it undefined). `createMoveContext` uses `allUnits ?? units` for occupancy so a wolf will not step onto the (enemy) Hero. Unblocks Seeker/Flee AI. Single source of truth, no adapter.
- [x] **Pack attack-on-adjacency (spec 06)** — `PackBehavior.tryAttack` makes each wolf bite an adjacent non-wolf living unit after moving (re-reads position post-move; reads `allUnits` to see the Hero). Dissolved followers keep their bite.
- [x] **Hero + wolf combat stats** — Hero is now `Damageable(30) + Damaging(10)`; PackLeader `Damaging(7)`, PackFollower `Damaging(5)`. HP/damage are not fixed by specs — chosen so wolves < bandits(8). Renderer (`shared/game_world.ts`) tears down a unit sprite on `TakeDamage` when its unit left `state.units`.
- [x] **Unit tests** — 128 tests. Added `core/units/damaging.test.ts`; expanded reducer (`TakeDamage`), `player_store` (`Attack` + `allUnits`), `pack_behavior` (attack), `wolf` (damage) tests.
- [x] **Wanderer NPC unit + Flee AI + Stage 1 wiring (specs 06, 09)** — `main/units/wanderer.ts` `Wanderer = Sightful(Movable(Unit, 3), 2)` — deliberately NO Damageable/Damaging, which is exactly what makes it un-attackable (`isDamageable`/`isDamaging` both false). `core/player/flee_behavior.ts` `FleeBehavior(store, {fleeFrom: string[]})`: each own unit steps via `directionAway` from the nearest unit owned by a `fleeFrom` id (reads `allUnits ?? units`), stays put when no step increases distance or when boxed in, never attacks, then dispatches `EndTurn`. `player_store` Attack translation now rejects non-damageable targets **before** spending the attacker's charge (`if (!isDamageable(targetEntry.unit)) break;`). `PlayerColor.GREEN` added; neutral `wandererPlayer` (id `"wanderer"`) registered **last** in `main/scenario.ts` so it acts after the wolves (spec 06 "Wanderer acts last"), spawns one `Wanderer` at new board section `wanderer_spawn` (board1.json tile (4,4), grass, deep-right away from Whirley's start), routed to `FleeBehavior` with `fleeFrom: [human.id]`. Renderer (`shared/game_world.ts`) tints the neutral hex green. Tests: `flee_behavior.test.ts` (6), `wanderer.test.ts` (6), `player_store` non-damageable-target (1). **160 tests, tsc clean, astro check 0 errors.**
- [x] **Occupied-tile + zero-budget enforcement in reducer (spec 02)** — the `Move` reducer is now the single source of truth for the two movement rules it can see globally: it rejects a move by a `Movable` unit with no budget (`!canMove()`) and a move onto a hex held by ANY other unit — **before** mutating the unit, so a rejected move never spends a movement point. Bounds/adjacency stay caller-enforced by design (the reducer has no tile/click context; existing Move tests deliberately run tile-less and jump >1 hex). Closes the human-path hole: `main/game_world.ts` `handleTileClick` would previously walk the Hero onto an adjacent enemy-occupied hex (`directionBetween` already blocks non-adjacent clicks; occupancy was unchecked). AI movers were already safe (`createMoveContext` filters occupancy), so no behavior change for them. `game.test.ts` end-condition tests now `hero.replenish()` before their raw teleport-Move (they never start a turn, so the hero had 0 budget). Tests: reducer occupied-hex + zero-budget rejection (2). **162 tests, tsc clean, astro check 0 errors.**
- [x] **Turn counter (spec 01, prerequisite for spec 07 turn-N triggers)** — `State.turn: number` (required field, all four State literals updated: `world.ts` init + the three test literals + editor store). Init `0`; the `StartTurn` reducer case returns `turn: state.turn + 1`, so the very first StartTurn yields `turn === 1` (spec 01 "the game begins by starting the first turn"). `EndTurn` does not touch it. The terminal-state guard already short-circuits `StartTurn` once `outcome` is set, so the counter freezes at stage end. **Semantics: a monotonic per-StartTurn count**, not a per-round count — with N players a full round advances `turn` by N. The Stage 1 "Turn 1 start" narrative beat maps cleanly (human is registered first, so turn 1 = human's first turn). **Future note for the narrative engine:** Stage 2/3 "Turn 3"/"Turn 4" beats are player-facing; when wiring them decide whether "Turn N" means the Nth StartTurn or the human's Nth turn (round) and map accordingly — the primitive is the raw StartTurn count. `Reset` does not yet reset `turn` (Reset has no reducer case at all); the "reset turn counter on stage reload" work stays under the Stage 2 "Stage reset on lose" item. Tests: reducer `StartTurn` describe — first-turn=1, increments each StartTurn, EndTurn no-op, frozen after GameEnd (4). **166 tests, tsc clean.**
- [x] **Win/lose conditions + GameEnd + terminal state (spec 05)** — `core/conditions/index.ts`: pure predicates `destinationReached(playerId, goalSections)` / `lastUnitDefeated(playerId)` + `evaluateEndConditions(state, {win, lose})` (lose-before-win precedence, OR within a kind). `GameEndEvent` now carries `outcome: GameOutcome ("win"|"lose")`. `State.outcome: GameOutcome | null` (required field — all State literals updated). Reducer: handles `GameEnd` (sets outcome) and **enforces terminal state** — rejects every gameplay action once `outcome !== null` (only `GameEnd`/`Reset` pass through). `Game.setEndConditions()` + `Game.onWorldUpdate` evaluates after each `Move`/`TakeDamage` and dispatches `GameEnd` (synchronously, before the observable drains). `main/scenario.ts` wires Stage 1 conditions (win: reach `village`; lose: last human unit dies) and emits `"gameEnd"`; `MainWorld` shows a victory/defeat indicator and stops accepting clicks. **board1.json**: tile (7,0) is now `sectionName: "village"` (far-right grass goal). Tests: `core/conditions/index.test.ts` (9), `core/game.test.ts` (5, full Game flow), reducer `GameEnd`/terminal tests. **147 tests, tsc clean, astro build clean.**

### Discovered while implementing (plan was stale)
- **Click-to-move + unit selection ARE already implemented** in `main/game_world.ts` `handleTileClick` (select friendly unit on click, then click a hex to move the selected unit via `Scenario.moveUnit`). The Stage 1 "Player input — click-to-move" / "Unit selection" items below are largely done; what remains is highlight-valid-moves, an explicit end-turn button, and click-to-attack.
- **Pre-existing broken WIP in `shared/game_world.ts`** (uncommitted `M` at session start): re-introduced the multi-layer "colored hex background + ship" unit rendering that commit 3405e97 had simplified away via sprite tint. It did not typecheck (`unitSprites: Map<number,Tile>` storing a `Container`; `.coordinates` assigned on a `Container`). Made the **minimal intent-preserving fix** to unblock the build: `Map<number, Container>` and dropped the invalid `.coordinates` assignment (position is driven by the tweened container x/y). **This maintainer WIP should be reviewed** — it conflicts with the latest committed rendering approach.
- **Pre-existing wrong import** `main/game_world.ts` imported `UnitPosition` from `../core/world` (not exported there) — fixed to `../core/board`.

---

## Architectural notes for future work

- **`allUnits` is how a behavior sees enemies.** The per-player proxy exposes `state.allUnits` (full roster) plus `state.units` (this player only). `FleeBehavior` (Wanderer, DONE) reads `allUnits` and takes an explicit `fleeFrom: string[]` (owner ids of the player) rather than "everyone who isn't me" — the Wanderer flees the player, not the wolves. **Seeker (bandit) should follow the exact same pattern**: read `allUnits`, target the nearest unit owned by the human player id (pass it in from the scenario). Raw (unproxied) state leaves `allUnits` undefined, so always `allUnits ?? units`.
- **Un-attackable = no `Damageable` mixin.** The Wanderer is made un-targetable purely by lacking `Damageable` (`isDamageable` false). Both attack paths already gate on it: `PackBehavior.tryAttack` filters `isDamageable`, and `player_store` Attack now rejects a non-damageable target before spending the charge. Any future "cannot be attacked" unit should just omit `Damageable`.
- **SPEC INCONSISTENCY (specs 06 vs 09) — unresolved.** Spec 06 says a wolf "attacks any adjacent non-wolf unit at the end of its move" (would include Whirley), but spec 09 says "The wolves do not target Whirley." The current implementation follows spec 06 (`PackBehavior.tryAttack` bites any adjacent damageable non-wolf, i.e. it WILL bite Whirley; it won't bite the non-damageable Wanderer). This contradicts spec 09's "walk past the pack / fight through if cornered" framing. **Decision needed:** either (a) wolves never target Whirley (pack is pure ambient hazard — makes stage trivial), or (b) accept spec 09's line as loose flavor and keep spec-06 behavior. Recommend (b) + tightening spec 09 wording. Do NOT silently rewrite specs without confirming the intended difficulty.
- **Occupancy + zero-budget are now enforced in the reducer (single source of truth).** `createMoveContext` still keeps AI off occupied tiles pre-dispatch, but the `Move` reducer independently rejects (a) a move by a `Movable` unit with `!canMove()` and (b) a move onto a hex held by any other unit — covering every mover (human + AI). **Bounds and adjacency remain caller-enforced**: the reducer has no tile/click geometry, existing Move tests run tile-less and teleport >1 hex, and both callers already handle geometry (human via `directionBetween` returning null for non-adjacent clicks; AI via `createMoveContext` bounds). **Minor follow-up:** `Scenario.onWorldUpdate`'s `Move` case deletes the mover from `unitsToMove` on every dispatch (the store notifies even when the reducer returns unchanged state), so a *rejected* human move onto an occupied hex still counts as "moved" for auto-end-turn bookkeeping. Harmless today (game state is correct; the unit just doesn't move), but revisit when click-to-attack lands — clicking an adjacent enemy should become an Attack, not a rejected Move.
- **Win/lose is evaluated in `Game`, not the reducer.** The reducer is pure and knows neither goal sections nor which player is human, so end conditions live in `core/conditions/` as pure predicates and `Game.onWorldUpdate` evaluates them after each `Move`/`TakeDamage`, dispatching `GameEnd`. `GameEnd` sets `state.outcome`, which the reducer then uses to reject all further gameplay (terminal state). Stage progression (win → load next stage, lose → reload) is the next increment — it hooks the `"gameEnd"` emitter event / `state.outcome`.
- **Narrative pauses via the serial Observable, not a flag.** A beat is shown by a world-observable subscriber (`MainWorld.onNarrativeUpdate`) that withholds `done()` until the dialog queue drains. Because the `Observable` publishes items serially (next item waits on `Promise.all` of all subscribers' `done`), this freezes everything — including a `GameEnd` the Game already queued behind a winning `Move`. **Stages 2/3 reuse this as-is**: write a `createStageNNarrative(...)` script (see `main/narrative/stage1.ts`), pass it to a fresh `NarrativeEngine`, done. New trigger kinds (e.g. a boss "unitDefeated" for the Bandit Captain — use `{ kind: "unitDefeated", predicate: isBanditCaptain }`) are already supported. **Stage reset on lose must call `narrative.reset()`** so beats replay on reload (that item lives under Stage 2).
- **One step per unit per turn.** Both `Explorer` and `PackBehavior` dispatch a single Move per unit even when the unit has >1 movement point. Matches spec wording ("moves one step") but means multi-point budgets are underused. Revisit if a behavior needs to close distance faster.

## Stage 1 — The Wreck (make it playable)

Priority: highest — entry point for everything else.

- [x] **Wolf Pack Leader unit** — done (see Completed). Stats `Damageable 20 / move 2 / sight 2`.
- [x] **Wolf Pack Follower unit** — done; `Wolf` renamed to `PackFollower`.
- [x] **Pack Leader AI** — done (wander, no-backtracking, attack-on-adjacency).
- [x] **Pack Follower AI** — done (step toward leader, stay if adjacent, `directionToward` handles blocked shortest path, attack-on-adjacency).
- [x] **Pack dissolution on leader death** — done; `PackBehavior` treats a missing/non-alive leader as dissolved → followers wander.
- [x] **Add `goal` section to board1.json** — done; tile (7,0) is `sectionName: "village"`.
- [x] **Wanderer NPC unit** — done; `Wanderer = Sightful(Movable(Unit, 3), 2)`, no combat mixins → un-attackable.
- [x] **Flee AI behavior** — done; `FleeBehavior` reads `allUnits`, flees nearest `fleeFrom`-owned unit via `directionAway`, never attacks. (Was "BLOCKED on proxy" — proxy `allUnits` unblocked it.)
- [x] **Finish Stage 1 scenario** — done; neutral `wandererPlayer` registered last, Wanderer spawns at `wanderer_spawn` (board1.json (4,4)), routed to `FleeBehavior`. All Stage 1 units now spawn per spec 09 acceptance criteria.
- [x] **Win condition check after Move** — done via `Game.onWorldUpdate` + `destinationReached`.
- [x] **Lose condition check after damage** — done via `Game.onWorldUpdate` + `lastUnitDefeated`.
- [x] **`GameEvent.GameEnd` with outcome field** — done; `GameEndEvent.outcome: GameOutcome`.
- [x] **Terminal state enforcement** — done; reducer rejects gameplay actions once `state.outcome` is set.
- [x] **Player input — click-to-move** — already implemented in `main/game_world.ts` `handleTileClick` (guarded by `isPlayerTurn`). Remaining: guard mid-animation (see note below).
- [x] **Unit selection** — already implemented; clicking a friendly unit sets `selectedUnit`.
- [x] **Highlight valid moves (spec 03) + End-turn button (spec 03) + Player attack action (spec 03/04, Stage 2 item below)** — `core/player/movement.ts` `validMoveDestinations`/`validAttackTargets` are the single pure source of truth (Movable+budget / Damaging+charge+adjacency+Damageable-target), reused by both the renderer's highlight and `game_world.ts`'s click decision so they can't drift. `MainWorld` adds a `highlightContainer` (green hex markers, world-space so they pan/zoom with the viewport) redrawn on every selection/turn/dialog change, and a screen-space End Turn button wired to `Scenario.endPlayerTurn()` — the human turn no longer auto-ends when units run out of moves (`scenario.ts`'s old `setTimeout` auto-end on `unitsToMove.size === 0` was removed). `handleTileClick` now branches on what's under the click: friendly unit → select, enemy/NPC unit → `Scenario.attackUnit()` if it's a legal target per `validAttackTargets` else no-op, empty hex → move. **204 tests, tsc clean.** **Known gap (not yet fixed):** no guard against clicks landing mid-move-tween (~600ms animation) — spec 03 "Clicks during... an animation... are ignored" is unimplemented; state is already correct when this happens (the reducer is synchronous, only the sprite hasn't visually caught up) so it's a minor UX/exploit surface, not a game-state bug.
- [x] **Stage 1 narrative events** — done; `main/narrative/stage1.ts` script wires all four spec-09 beats (Turn 1 start, within sight of Wanderer, reaches village, Whirley defeated) verbatim. See Completed.
- [x] **Dialog component** — done; `shared/dialog_box.ts` modal overlay (speaker + wrapped text, click-to-advance one line, full-screen backdrop that swallows board clicks). See Completed. See `specs/07-narrative-events.md`.
- [x] **Turn counter** — done; `State.turn: number` (required), init `0`, incremented by one in the `StartTurn` reducer case (turn `1` on the very first StartTurn). See Completed.
- [x] **Occupied-tile enforcement** — done in the `Move` reducer (rejects occupied-hex + zero-budget moves for every mover). See Completed. `specs/02-movement-system.md`.

---

## Stage 2 — The Gate

Depends on: Stage 1 playable, combat system, bandit unit.

- [x] **Bandit unit** — `main/units/bandit.ts`: `Bandit = Sightful(Movable(Damaging(Damageable(Unit, 25), 8), 2), 1)` — 25 HP / 8 damage / move 2 / sight 1, measurably higher HP and damage than the wolf pack leader (20 HP / 7 damage), matching spec 10's "Bandits have higher HP and deal more damage per attack than wolves." Exported from `main/units/index.ts`. Not yet spawned anywhere (waits on Stage 2 board + scenario). Tests: `main/units/bandit.test.ts` (10). **204 tests, tsc clean.**
- [x] **Damaging mixin** — `Damaging(Base, damage, attacksPerTurn=1)` in `core/units/damaging.ts`. Wolves/Hero already use it.
- [x] **Player attack action** — done; see the spec 03 entry above (Stage 1 section) — `game_world.ts` `handleTileClick` dispatches `Scenario.attackUnit()` when an adjacent, legal enemy is clicked.
- [x] **`PlayerAction.Attack` → `GameEvent.TakeDamage`** — done in `createPlayerStore` (resolves target by hex, validates friendly-fire/adjacency/charge).
- [x] **Enemy attack → `GameEvent.TakeDamage`** — wolves emit it via `PackBehavior.tryAttack`; bandits will reuse the same `Attack` action.
- [x] **Death handling in reducer** — `TakeDamage` removes the resolved target when `!isAlive()`. Win/lose check is a separate (now unblocked) item.
- [x] **Attack action charge** — `Damaging` carries one attack/turn (configurable), replenished at turn start, blocked when exhausted.
- [x] **Seeker AI behavior** — `core/player/seeker_behavior.ts` `SeekerBehavior(store, { huntFor: string[] })`: mirrors `FleeBehavior`'s shape (reads `allUnits ?? units` for the full roster, targets filtered to owner ids in `huntFor`). Per unit: find the nearest hunted unit; if already adjacent (`hexDistance === 1`) it does **not** move — it attacks instead via `PlayerActionType.Attack`, and only when `isDamaging(unit) && canAttack()` (spec 06 "A bandit attacks only if it has an attack action remaining for the turn" — out of charges means the unit does nothing that turn, it does not fall back to moving); otherwise it takes one step via `directionToward` (boxed-in/no-budget → skip, matches `FleeBehavior`/`PackBehavior`). Each unit acts independently — no shared coordination state (unlike `PackBehavior`'s leader-follower memory). Not yet wired to any scenario — waits on Stage 2 scenario/board. Tests: `core/player/seeker_behavior.test.ts` (8: end-turn-always, move-toward, adjacent-attacks-not-moves, no-charge-does-neither, hunts-correct-owner, boxed-in, no-budget, two-bandits-act-independently). **212 tests, tsc clean.** Also extracted the `makeTiles`/`cubeAt` test board builders — duplicated verbatim across `flee_behavior.test.ts`, `pack_behavior.test.ts`, and this new file — into `core/grid/test_helpers.ts`, now imported by all three (crossed rule-of-three; `makeStore` variants were left alone since their shapes genuinely differ).
- [x] **Board for Stage 2** — `main/boards/board2.json`: 8×6 grid (matches board1.json's data shape — `rows`/`cols`/`tiles`, `type` always `Terrain.WATER` since it's the only enum value, `textureName` "grass"/"water" is what actually reads as a road), two-row "road" band (y=2,3) flanked by water. Sections: `spawn_a` (0,2) at the road entrance/far side from the gate, `bandit_1`/`bandit_2`/`bandit_3` (2,2)/(4,3)/(6,2) spread between entrance and gate, `wanderer_spawn` (6,3) adjacent to `gate` (7,3) — matches spec 10's "near the gate wall, on the same side as the bandits." Not yet wired into any scenario (no bandit spawning, no Stage 2 scenario.ts) — waits on the Stage 2 scenario item below. Tests: `main/boards/board2.test.ts` (4: board size, all six sections present exactly once, bandits `hexDistance`-between entrance and gate, Wanderer closer to gate than entrance). **216 tests, tsc clean.** Minor note for whoever wires the scenario: `bandit_3` (6,2) and `wanderer_spawn` (6,3) are adjacent — spec-compliant but visually tight, worth a glance once units are actually spawned.
- [x] **Stage 2 scenario (spawns/behaviors/win-lose only — board + narrative wiring are separate items below)** — Generalized `Scenario` (`main/scenario.ts`) from hard-coded Stage 1 content into a `StageDefinition`-driven class (specs/08 "the engine accepts a stage definition and configures itself accordingly; it does not hard-code stage content"), rather than duplicating ~150 lines for Stage 2. New `main/stages/stage.ts`: `StageDefinition { player, playerSpawns, enemies: EnemyRoster[], winSection }`, `EnemyRoster { player, spawns, turnEventName, createBehavior(store) }`. Deliberately excludes board and narrative — board is chosen one layer up at `new Game(board)` construction (main/index.ts), narrative is wired separately by MainWorld (main/narrative/*); neither is consumed by Scenario, so including them here would be unused/speculative (this makes `StageDefinition` a **partial** implementation of spec 08's six-part "Stage Definition" list by design — board + narrative are the two elements it doesn't cover). `main/stages/stage1.ts` `createStage1Definition()` reproduces Stage 1 exactly (verified behavior-preserving — same players/spawns/win section "village"/pack memory persistence). `main/stages/stage2.ts` `createStage2Definition()`: one Hero (not two), three Bandits via `SeekerBehavior({huntFor:[player.id]})` (not `PackBehavior`), Wanderer via `FleeBehavior` registered last (spec 06 "Wanderer acts last"), `winSection: "gate"`. `game_world.ts` updated to `new Scenario(this.game, createStage1Definition())` — **MainWorld still only ever loads Stage 1**; nothing yet constructs a Stage-2 `Game`/board or listens for `"banditTurn"` (that's the "Stage progression" item below). Tests: `main/scenario.test.ts` (7, new — Scenario had zero tests before this) covering both definitions' spawns, turn-order handoff (via emitted turn events — asserting `currentPlayer` after a flush is unreliable on a minimal test board since every CPU faction no-ops and the turn cascades multiple times before the flush settles), and win-section correctness (Stage 2 reaching "village" must NOT win). **223 tests, tsc clean.** Known coupling risk for whoever wires MainWorld to Stage 2: `EnemyRoster.turnEventName` (e.g. `"banditTurn"`) is a bare string with nothing tying it to the `.on(...)` listener that must exist in `game_world.ts` — a typo on either side compiles fine and just silently never fires the turn banner.
- [x] **Stage 2 narrative events** — `main/narrative/stage2.ts` `createStage2Narrative(humanId, wandererId)`, all five spec-10 beats verbatim, mirroring `stage1.ts`'s factory shape: `stage2-turn-1` (3 lines), `stage2-turn-3` (2 lines), `stage2-wanderer-sighted` (proximity), `stage2-reached-gate` (tileReached "gate", `endsStage: "win"`), `stage2-whirley-defeated` (lastUnitDefeated, `endsStage: "lose"`). **Real bug caught mid-implementation:** spec 10's "Turn 3 start" is player-facing (the human's 3rd turn), but the narrative engine's `{kind:"turn"}` trigger matches the raw, monotonic per-StartTurn count — every player's StartTurn increments it, not just the human's. With 3 players registered per round (human, bandits, wanderer), a literal `turn: 3` would misfire during the **Wanderer's very first turn** instead of the human's third. Fixed by extracting `turnForPlayer(playerIndex, playerCount, n)` into `core/narrative/index.ts` (the general "player's Nth turn → raw StartTurn count" translation, since this ambiguity applies to any multi-player stage, not just Stage 2) — `turnForPlayer(0, 3, 3) === 7`. `/code-review` caught the first draft encoding this as a bare `HUMANS_THIRD_TURN = 7` literal justified only by a comment, plus a test that re-derived it via a full `Game` simulation embedded in a narrative-script test (Feature Envy / testing the wrong module); replaced with the named helper (now independently unit-tested in `core/narrative/index.test.ts`, 4 tests) and a one-line assertion that the script actually uses it. Not yet wired to `MainWorld` (no `NarrativeEngine` constructed from it yet) — waits on Stage progression. Tests: `main/narrative/stage2.test.ts` (4). **231 tests, tsc clean, production build clean.**
- [x] **Reset primitive (prerequisite for both items below)** — `GameEventType.Reset` had a case in `game_event.ts`/the terminal-state guard but **no reducer implementation at all** (silently fell through to `default: return state`). Added it: clears every per-playthrough field (`players`, `units`, `turn`, `currentPlayer`/`currentPlayerIndex`, `revealedTiles`, `outcome`) but **keeps the board** (`tiles`/`worldWidth`/`worldHeight`/`cols`/`rows`) — a same-board reload, not a new stage. Extracted the cleared-field literal into `core/world_defaults.ts` `EMPTY_PLAYTHROUGH_STATE`, shared with `World`'s constructor (both now derive from the same source instead of two hand-synced copies — `/code-review` caught the original duplication). `players` is cleared, not just `units`: `PlayerJoin` appends with no dedup-by-id, so re-adding same-id players onto a stale roster would silently double `rotate()`'s player count. `main/scenario.ts` `Scenario.reload(definition)`: dispatches `Reset` then calls `start()` again with a (possibly different) `StageDefinition` — reuses the same `Scenario` instance rather than constructing a new one, since `shared/observable.ts`'s `Observable` has no unsubscribe (a second instance would double-drive every future turn). `shared/game_world.ts` renderer: destroys every unit sprite and stops any in-flight move tween on `Reset` (mirrors the existing per-unit `TakeDamage`-death cleanup), otherwise old sprites would linger as visual ghosts after a reload. **Board-preserving only** — swapping to a stage with a genuinely different board (Stage 1 → Stage 2) needs a new `Game`/`World` and a rebuilt renderer around it, which this does not attempt. Tests: `core/reducers/index.test.ts` (+5), `main/scenario.test.ts` (+3, including a regression test that `reload()` actually swaps win conditions, not just spawns). No test coverage for the `game_world.ts` Pixi hunk — no Pixi testing convention exists anywhere in this repo to extend; verified instead via `tsc` + a full `astro check && astro build`. **239 tests, tsc clean, production build clean.** `/code-review` clean on both axes after the `EMPTY_PLAYTHROUGH_STATE` fix.
- [ ] **Stage progression** — after Stage 1 win GameEnd event, load Stage 2. Needs: `MainWorld`'s `"gameEnd"` listener to call something on a win outcome; since Stage 2 has a **different board** than Stage 1, `Scenario.reload()` alone is insufficient (it's board-preserving) — this needs a new `Game`/`World` constructed from `board2.json` and the Pixi renderer (terrain/fog/viewport, not just units) rebuilt around it. Also needs a `NarrativeEngine` swap (`createStage2Narrative`) and a listener for Stage 2's `"banditTurn"` event (see the Stage 2 scenario entry's noted coupling risk above). See `specs/08-stage-system.md`.
- [x] **Stage reset on lose** — `main/game_world.ts`: the `"gameEnd"` listener's lose branch now passes `() => this.reloadCurrentStage()` as an `onComplete` callback to `showTurnIndicator("Defeated...", ...)`, so the reload fires once the indicator's own fade-out finishes (no independent `setTimeout` to keep in sync with that animation — one less thing to drift). `reloadCurrentStage()` calls `this.narrative.reset()` **then** `this.scenario.reload(createStage1Definition())` — order matters, since `reload()` synchronously dispatches a fresh `StartTurn` that the (now-cleared) narrative engine needs to see in order to replay the Turn-1 beat. Verified (by tracing, not a browser — see below) that the lose-triggering `stage1-whirley-defeated` dialog can never still be open when this fires: `GameEnd` is a separate action from the `TakeDamage` that causes it, and the world observable won't advance to the queued `GameEnd` until the dialog's `done()` — held via `pendingNarrativeDone` — actually fires, i.e. the dialog is already dismissed by construction. `/code-review` flagged a real fragility: `TWEEN.js`'s `.stop()` (used when a *second* `showTurnIndicator()` call interrupts a still-fading one) does **not** invoke `onComplete`, so a future indicator call reachable from the terminal state would silently drop the pending reload — currently impossible (the reducer's terminal-state guard rejects everything that could trigger another indicator once `outcome` is set), so fixing it now would be speculative generality; documented the invariant explicitly in a comment instead of building a more robust callback-survives-interruption mechanism nobody needs yet. Hardcoded to Stage 1 (`reloadCurrentStage` is not yet generic — MainWorld has no "which stage is active" concept, that's Stage progression's job). **No test coverage for this file** — same as the rest of `shared/`/`main/game_world.ts`, no Pixi testing convention exists anywhere in this repo; verified via `tsc` + full `astro check && astro build` instead. **239 tests (unchanged — this is pure Pixi wiring, the underlying `Scenario.reload`/`Reset` primitives were already tested in the prior commit), tsc clean, production build clean.** `/code-review` clean on both axes after the fragility-comment fix.

---

## Stage 3 — Let's Be Reasonable

Depends on: Stage 2 fully playable, Bandit Captain unit.

- [ ] **Bandit Captain unit** — `BanditCaptain = Sightful(Movable(Damageable(Unit, 40), 2), 2)` with `damage = 12`. Visually distinct from standard bandits. See `specs/11-stage-3.md`.
- [ ] **Board for Stage 3** — camp map with Wanderer cornered at far end. Sections: `spawn_a`, `campfire` (goal), `captain_spawn`, `bandit_4`, `bandit_5`, `bandit_6`, `wanderer_spawn`. Wanderer spawn is enclosed on three sides. See `specs/11-stage-3.md`.
- [ ] **Stage 3 scenario** — loads Stage 3 board; spawns Whirley + Bandit Captain + 3 Bandits + Wanderer; uses Stage 3 narrative script. See `specs/11-stage-3.md`.
- [ ] **Stage 3 narrative events** — dialog at: Turn 1 (3 lines), Turn 4, Bandit Captain defeated (2 lines), Wanderer sight range, Whirley reaches campfire (4 lines), Whirley defeated. See `specs/11-stage-3.md`.
- [ ] **Game completed state** — after Stage 3 win, no further stages load; completed state blocks all gameplay input. See `specs/08-stage-system.md`.
- [ ] **Stage progression Stage 2 → Stage 3** — after Stage 2 win, load Stage 3. See `specs/08-stage-system.md`.

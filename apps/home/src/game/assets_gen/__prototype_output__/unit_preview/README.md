# Unit sprite preview — prototype output

Generated by `../../unit_preview.prototype.ts` for gh issue #190.
Throwaway design-review artifact, not a production asset — lives on the
`prototype/unit-sprite-preview-190` branch only, not on the main effort
branch.

- `unit_roster.png` — the 5 sprites side by side on a neutral backdrop
  (reading order: hero, wanderer, wolf, bandit, banditCaptain).
- `unit_roster_on_tint.png` — the same roster composited onto the
  owner-color tint hex exactly as `game_world.ts` draws it today (flat-top
  hex, radius 50, 60% alpha), using the blue player color.

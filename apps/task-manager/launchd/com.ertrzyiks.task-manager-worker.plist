<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  LaunchAgent for the task-manager Mac worker (apps/task-manager/src/worker.ts, #249/#251).
  Keeps the worker consuming the `extract-action-items` BullMQ queue whenever the user is
  logged in, and restarts it automatically if it crashes or exits unexpectedly (KeepAlive,
  subject to launchd's default crash-loop throttling).

  This file is a TEMPLATE. The one REPLACE_ME_* placeholder below must be substituted with a
  real, machine-specific value before installing — see the "macOS LaunchAgent" section of
  apps/task-manager/README.md for the install steps. Never commit a copy of this file with
  real values filled in; the installed copy lives outside git, under ~/Library/LaunchAgents/.

    - REPLACE_ME_REPO_PATH: absolute path to this repo checkout, e.g.
      /Users/you/code/ertrzyiks.me

  Runs the standalone `dist-bin/task-manager-worker` executable directly — NOT `node
  dist/worker.js` or `pnpm run worker` (the dev-only entrypoint). That executable is produced
  by the "release:worker" package.json script (scripts/release-worker.mjs), which bundles
  worker.ts via esbuild and packages it with @yao-pkg/pkg. Run that script (not just `build`)
  before installing/reloading this LaunchAgent — see README.md's "macOS LaunchAgent" section
  for why a standalone binary is used instead of the shared `node` interpreter: it lets the
  Keychain access grant below be scoped to this one program.

  No secrets appear here. REDIS_URL, GMAIL_CLIENT_ID, and GMAIL_CLIENT_SECRET — like the
  Gmail refresh token already did — are read from the macOS Keychain at startup (src/
  keychain.ts) instead of being passed as EnvironmentVariables. release-worker.mjs
  (re-)provisions all four Keychain items on every run, trusting this exact binary so
  `security find-generic-password` doesn't prompt for authorization at every restart.

  Optional env var overrides (GMAIL_KEYCHAIN_ACCOUNT, GMAIL_KEYCHAIN_SERVICE,
  LM_STUDIO_BASE_URL) are omitted here since worker.ts already falls back to sensible
  defaults (see src/worker.ts) — add them under EnvironmentVariables below only if you need
  to override one.
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.ertrzyiks.task-manager-worker</string>

  <key>ProgramArguments</key>
  <array>
    <string>REPLACE_ME_REPO_PATH/apps/task-manager/dist-bin/task-manager-worker</string>
  </array>

  <key>WorkingDirectory</key>
  <string>REPLACE_ME_REPO_PATH/apps/task-manager</string>

  <key>RunAtLoad</key>
  <true/>

  <key>KeepAlive</key>
  <true/>

  <key>StandardOutPath</key>
  <string>REPLACE_ME_REPO_PATH/apps/task-manager/launchd/worker.log</string>

  <key>StandardErrorPath</key>
  <string>REPLACE_ME_REPO_PATH/apps/task-manager/launchd/worker-error.log</string>
</dict>
</plist>

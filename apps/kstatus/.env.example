DATABASE_PATH=./data/dev.sqlite
PORT=3000
# Both optional — leave unset in dev to browse /admin with no auth prompt at all.
# Set both to exercise the Basic Auth guard locally the same way it runs in production.
KSTATUS_ADMIN_BASIC_AUTH_USERNAME=
KSTATUS_ADMIN_BASIC_AUTH_PASSWORD=
